# Specification

## Summary
**Goal:** Replace all occurrences of "GrowthBridge Media" (and its variants) with "HP BRIDGE MEDIA" across the entire frontend codebase.

**Planned changes:**
- In `Navbar.tsx`, replace every instance of "GrowthBridge Media", "GrowthBridge", or "Growth Bridge Media" with "HP BRIDGE MEDIA" so the navbar logo reads correctly in both transparent and scrolled states.
- In `index.html`, update the `<title>` tag to "HP BRIDGE MEDIA".
- In `Footer.tsx`, replace the footer logo text and copyright line with "HP BRIDGE MEDIA".
- In `HeroSection.tsx`, `AccessDeniedScreen.tsx`, `AdminDashboard.tsx`, and any other frontend `.tsx`/`.ts`/`.html` files, replace all remaining occurrences of the old company name with "HP BRIDGE MEDIA".

**User-visible outcome:** Every visible instance of "GrowthBridge Media" on the site — navbar, footer, browser tab title, hero section, and access-denied screen — will display "HP BRIDGE MEDIA" instead.
